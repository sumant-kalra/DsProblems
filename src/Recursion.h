#ifndef RECURSION_H
#define RECURSION_H

namespace recursion
{
    unsigned long long int factorial(unsigned short int number);
    bool isPalindrome(const std::string &word);

    double power_r1(double n, int p);
    double power(double n, int p);
    double power_r2(double n, int p);

}

#endif